<Project>
  <PropertyGroup>
    <VersionMain>1.0</VersionMain>
    <VersionPrefix>0.1</VersionPrefix>
    <VersionSuffix>-preview</VersionSuffix>
    <VersionSuffixVersion>1</VersionSuffixVersion>
    <Version>$(VersionMain).$(VersionPrefix)$(VersionSuffix)$(VersionSuffixVersion)</Version>
    <AssemblyVersion>$(VersionMain).$(VersionPrefix)</AssemblyVersion>
	<FileVersion>$(VersionMain).$(VersionPrefix)</FileVersion>
	<RepositoryPath>$(MSBuildThisFileDirectory)..\</RepositoryPath>
	<BuildBranch>develop</BuildBranch>
  </PropertyGroup>
</Project>
